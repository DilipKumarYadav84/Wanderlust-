<% layout('layouts/boilerplate') %>
  
    <div class="row mt-3">
        <div class="col-8 offset-3">
              <h3><%= listing.title %></h3>
        </div>
    <div class="card col-6 offset-3 show-card listing-card">
        <img src="<%= listing.image && listing.image.url ? listing.image.url : '' %>" class="card-img-top show-img" alt="listing image">
  <div class="card-body">
    <p class="card-text">
    <%= listing.description %><br>
     &#8377;<%= listing.price ? listing.price.toLocaleString("en-IN") : 0 %> <br>
    <%= listing.country %><br>
    <%= listing.location %>
    </p> 
  </div>
    </div>
   
    <br />
    </div>
    <div class="col-8 offset-3">
        <div class="btns">
            <a href="/listings/<%= listing._id %>/edit" class="btn btn-dark edit-btn">Edit</a>
            <form method="POST" action="/listings/<%= listing._id %>?_method=DELETE">
                <button class="btn btn-dark">Delete</button>
            </form>
        </div>
    </div>
    <div class="col-8 offset-3 review-section">
        <h4>Leave a Review</h4>
        <form method="POST" action="/listings/<%= listing._id %>/reviews" novalidate class="needs-validation">
            <div class="mb-3 mt-3">
                <label for="comment" class="form-label">Comments</label>
                <textarea class="form-control" id="comment" name="review[comment]" rows="5" required></textarea>
                <div class="invalid-feedback">
                Please leave some comments.
            </div>
            </div>

        <div class="mb-3 mt-3">
            <label for="rating" class="form-label">Rating <output id="ratingValue">3</output></label>
            <input type="range" id="rating" name="review[rating]" min="1" max="5" class="form-range" value="3" step="1" oninput="ratingValue.value = this.value" required>
            <div class="invalid-feedback">
                Please select a rating.
            </div>
        </div>
            <button class="btn btn-outline-dark">Submit</button>
        </form>
        <hr />
        <p><strong>All Reviews</strong></p>
        <div class="row">
         <% for (let review of listing.reviews) { %>
            <div class="card col-5  ms-3 mb-3 review-card">
                <div class="card-body">
                    <h5 class="card-title">Jane Doe</h5>
                    <p class="card-text"> <%= review.comment %></p>
                    <p class="card-text">
                        Rating: <%= review.rating %> star </p>
                </div>   
               <form class="mb-3" method="POST"  action="/listings/<%= listing._id %>/reviews/<%= review._id %>?_method=DELETE">
                    <button class="btn btn-dark">Delete</button>
               </form>
            </div>
        <% } %>
        </div>

    </div>
    </div>
    
    
